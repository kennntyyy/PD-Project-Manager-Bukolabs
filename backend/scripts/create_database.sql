CREATE DATABASE IF NOT EXISTS `project_management`
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

-- Optional: grant privileges for local development (uncomment and edit user if needed)
-- GRANT ALL PRIVILEGES ON `project_management`.* TO 'root'@'localhost';
-- FLUSH PRIVILEGES;
